import * as React from 'react'
import { useBlockProps } from '@wordpress/block-editor';

function saveBlockName( props ) {
  const { attributes, setAttributes } = props;

  return ();
}

export default saveBlockName;
