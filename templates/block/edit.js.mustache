import * as React from 'react'
import { useBlockProps } from '@wordpress/block-editor'
import './editor.scss'

const Edit{{slugPascalCase}} = ({ attributes, setAttributes }) => {
  const {  } = attributes
  const blockProps = useBlockProps()

  return <p { ...blockProps }>This is your custom block</p>
}

export default Edit{{slugPascalCase}}
